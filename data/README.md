# Data Directory

Place small, shareable datasets here for use in notebooks. If data is too large, provide a script or instructions to download/generate it.

Example: `movies.csv`, `kg_sample.ttl`, etc.

